# Component Library

Uses Base UI components.

When browsing their docs, append `.md` to URLs. For example: access `https://base-ui.com/react/components/accordion.md` instead of `https://base-ui.com/react/components/accordion`.
